db.url=jdbc:postgresql://localhost:5432/backtest
db.user=backtest
db.password=backtest
db.pool.maxSize=10
db.pool.minIdle=2
db.pool.connectionTimeoutMs=10000
default.initial.capital=10000.0
default.commission.type=percentage
default.commission.value=0.001
default.slippage.type=percentage
default.slippage.value=0.0005

# Number of model-version subdirs to keep per cache key when `train` saves a
# new model (oldest versions deleted automatically). Set to 0 or negative to
# disable retention and keep unlimited history. Override per-invocation with
# `train --keep-last N`.
model.retention.keepLastN=5
