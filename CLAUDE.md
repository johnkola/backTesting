Project docs live in `README.md` (roadmap, model/feature caches, storage, deployment) and `ARCHITECTURE.md` (domain types, repos, web layer). Start there.

Note: the `/claude` web route is a 301 redirect to `/architecture` for back-compat — there is no separate Claude-facing doc page.
