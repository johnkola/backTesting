{
  "name": "backtest-web-server",
  "version": "0.1.0",
  "private": true,
  "description": "Backtest API: reads Postgres, proxies the loader and the engine, and serves the project docs as rendered HTML for the client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "express": "^4.21.2",
    "marked": "^14.1.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.11.0",
    "eslint-plugin-import-x": "^4.17.1",
    "globals": "^17.12.0"
  }
}
